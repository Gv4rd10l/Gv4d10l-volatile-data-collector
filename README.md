# Gv4d10l-volatile-data-collector
A bash script tool to collect data from volatile memory on Linux. Follow these steps before using the tool :
1/ Open the .sh file and change the thrid line : OUTPUT_DIR="/media/usb/volatile_data"
replace /media/usb/volatile_data by the path to where you want to keep your capture data.
